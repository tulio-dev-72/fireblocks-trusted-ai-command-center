/**
 * Web app — browser-safe env vars only.
 * Never put Fireblocks keys, JWT secrets, or LLM API keys here.
 */
# Public API base URL (set at build time on Vercel)
VITE_API_URL=https://api.your-domain.com

# Optional: Bearer token for authenticated API calls in production.
# Prefer SSO/JWT issuance — dev-token is rejected when NODE_ENV=production on the API.
# VITE_API_TOKEN=
