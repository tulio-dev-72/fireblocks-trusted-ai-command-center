services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: taicc
      POSTGRES_PASSWORD: taicc
      POSTGRES_DB: taicc
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
